// TODO: Define Prisma schema
// Models: User, NannyProfile, ParentProfile, Child, EmergencyContact,
//         Booking, Review
// Requires: postgis extension for geospatial nanny search

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
