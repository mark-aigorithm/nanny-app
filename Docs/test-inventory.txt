================================================================================================
NANNYAPP — AUTOMATED TEST INVENTORY
Generated 2026-09-20 from git HEAD 69bda24
================================================================================================

SUMMARY
------------------------------------------------------------------------------------------------
  863 tests  in  77 files   BACKEND — unit tests (Jest)
  154 tests  in  26 files   BACKEND — integration tests (Jest, real PostGIS + Auth emulator + Paymob fake)
   43 tests  in   7 files   SHARED PACKAGE — schema tests (Vitest)
   27 tests  in   7 files   ADMIN WEB — unit / component tests (Vitest + Testing Library + MSW)
   68 tests  in  13 files   ADMIN WEB — end-to-end tests (Playwright, real browser + backend)
  173 tests  in  38 files   MOBILE APP — unit / component tests (Jest + React Native Testing Library)
   18 tests  in  18 files   MOBILE APP — end-to-end device flows (Maestro, Android emulator)
------------------------------------------------------------------------------------------------
 1346 tests  in 186 files   TOTAL

How to read this file
  • Each section is one test tier. Files are listed with their path in the repository.
  • Indented, un-dashed lines are test suites (describe blocks); "- " lines are individual tests.
  • [parameterised] = one entry that runs once per row of a data table (counted once).
  • [skip] / [todo] = present in the codebase but not executed today.
  • Maestro flows drive the real Android app on an emulator; one flow = one end-to-end scenario.
    Files starting with "_" are shared building blocks, not scenarios, and are omitted.
  • Backend integration tests and both E2E tiers run against real services (PostGIS, Firebase Auth
    emulator, Mailpit, and a Paymob payment fake that serves the real checkout page and signs webhooks).

================================================================================================
BACKEND — unit tests (Jest)
863 tests in 77 files
================================================================================================

apps/backend/src/__tests__/address.service.test.ts  (18 tests)
    createAddress
        - makes the first address the default even when the flag is not sent
        - adds a second address as non-default when the flag is not sent
        - clears the previous default in the same transaction when asked to be the default
        - serialises coordinates as numbers and dates as ISO strings
    updateAddress
        - 404s on an address the user does not own or has deleted
        - writes only the fields sent
        - refuses to un-default the current default — pick another one instead
        - promoting an address clears the previous default first
    setDefaultAddress
        - clears the others and returns the list with the new default first
    deleteAddress
        - soft-deletes and promotes the oldest survivor when the default goes
        - leaves the default alone when a non-default address goes
        - 404s on an address that is not the user’s
    upsertNannyAddress
        - rewrites her existing default in place
        - creates a default "Home" when she has none
    the signed-in user’s own addresses
        - lets a nanny read her address but not write one — support edits it
        - lets a mother add to her address book
        - 404s a signed-in Firebase user with no application row
    toBookingAddressSnapshot
        - copies every field plus the source id, with numeric coordinates

apps/backend/src/__tests__/admin-booking-edit.service.test.ts  (3 tests)
    applyBookingEdit — balance-due amount
        - charges the mother the full difference (new total − paid), not the per-hour rate
        - raises no balance-due obligation when the booking had not been paid yet
    previewBookingEdit — balance-due amount
        - previews the difference the mother owes, not the per-hour rate

apps/backend/src/__tests__/admin-booking.service.test.ts  (15 tests)
    approveBooking
        - approves PENDING → APPROVED even when the nanny DECLINED
        - rejects approving an unclaimed booking (no nanny assigned)
        - rejects approving a booking that is not PENDING
    rejectBooking
        - cancels the booking with a reason and notifies both parties
    setBookingStatus
        - is blocked when the booking is COMPLETED (locked)
        - applies a valid override and stamps the admin audit trail
        - rejects an invalid transition (PENDING → IN_PROGRESS)
    updateBookingTimes
        - recomputes duration + price from the new window and stamps the admin
        - rejects editing a COMPLETED booking (locked)
        - rejects a window shorter than the minimum duration
    listAdminBookings (paginated)
        - maps discountAmount and the applied promo code
        - reports a null promo code when none was applied
        - applies skip/take for the requested page and returns pagination meta
    getAdminBooking (detail)
        - returns the full breakdown, payment record, and a null pointsRedeemed
        - throws when the booking does not exist

apps/backend/src/__tests__/admin-id-review.service.test.ts  (5 tests)
    listIdReviews
        - lists parents only — a nanny is reviewed on her detail page, never here
        - applies the approval-status filter when not ALL
        - flips to newest first when the caller asks for it
        - maps a row: id is the User id the mother endpoints are keyed by
        - drops the "-" placeholder last name from the display name

apps/backend/src/__tests__/admin-marketplace.service.test.ts  (13 tests)
    listMarketplaceListings
        - defaults the queue to pending, oldest submission first
        - drops the status filter for ALL
    approveListing
        - publishes the listing, stamps the reviewer and notifies the seller
        - is idempotent on an already-approved listing
        - refuses a non-admin caller
        - 404s on a listing that does not exist
    rejectListing
        - stores the reason and tells the seller what to fix
        - takes down an already-approved listing
        - refuses to reject an official listing
    createOfficialListing
        - publishes an approved, pinned listing authored by the admin
    updateOfficialListing / deleteOfficialListing
        - rejects editing a seller listing through the official endpoint
        - soft-deletes an official listing and its comments
        - refuses to delete a seller listing

apps/backend/src/__tests__/admin-mother.service.test.ts  (16 tests)
    listAdminMothers
        - queries only mother accounts, newest first, applying skip/take
        - flips to oldest first when the caller asks for it
        - filters by verification status when not ALL
        - maps rows to the AdminMother DTO
        - drops the "-" placeholder last name from the display name
    getAdminMother
        - returns the mother DTO for an existing account
        - includes the raw first/last name split for the edit form
        - throws when the mother does not exist
    approveMother
        - marks the ID APPROVED and clears any rejection reason
        - rejects re-approving an already approved mother
    rejectMother
        - nulls the ID URLs, deletes the Storage files, and stores the reason
        - rejects re-rejecting an already rejected mother
    updateAdminMother
        - updates the provided fields and returns the detail DTO
        - stores an empty last name as the "-" placeholder
        - writes only the fields that were provided
        - throws and does not write when the mother does not exist

apps/backend/src/__tests__/admin-nanny-update.test.ts  (4 tests)
    updateAdminNanny
        - resolves the nanny by profile id, writes the given fields inside a transaction, and returns the refreshed detail DTO
        - throws notFound for an unknown nanny profile id and never opens a transaction
        - passes photo and date of birth straight through to the writer
        - exposes the home pin on the detail DTO as numbers, read off her address row

apps/backend/src/__tests__/admin-nanny.service.test.ts  (14 tests)
    listAdminNannies
        - exposes both sides of the ID document in the DTO, with pagination meta
        - returns null ID urls when the nanny has not uploaded documents
        - orders by the requested direction — newest first by default, oldest on request
    getAdminNanny (detail)
        - exposes the userId and aggregates lifetime earnings from COMPLETED bookings
        - reports zero earnings when the nanny has no completed bookings
        - throws when the nanny does not exist
    approveNanny
        - flips the user status to APPROVED and clears the rejection reason
        - rejects approving an already approved nanny
    rejectNanny
        - nulls the ID urls, deletes the Storage files, and stores the reason
        - rejects re-rejecting an already rejected nanny
    setNannySkills
        - throws notFound (404) when the nanny does not exist
        - throws badRequest (400) when a skill id is unknown or inactive
        - creates new links, reactivates soft-deleted, and removes unwanted
        - clearing all skills soft-deletes every active link and creates none

apps/backend/src/__tests__/admin-operator.service.test.ts  (13 tests)
    createAdminUser
        - stores the granted sections on an operator
        - stores nothing for a full admin — their reach comes from the role
        - refuses an email that is already taken
    updateAdminUser
        - re-scopes an operator
        - ignores a permission map sent for a full admin
        - splits a new name into first and last
        - refuses to touch the superuser
        - 404s on an account that does not exist
    deleteAdminUser
        - soft-deletes and disables the Firebase account
        - refuses to remove the acting account
        - refuses to remove the superuser
    listAdminUsers
        - includes operators alongside admins
    listConsoleUserIdsForSection
        - notifies only the accounts that can open the section

apps/backend/src/__tests__/admin-package-purchase.service.test.ts  (4 tests)
    - paginates and maps buyer + hoursConsumed
    - builds the where clause from status + search, and reuses it for count
    - detail includes the ledger, newest first
    - throws 404 when the purchase is missing

apps/backend/src/__tests__/admin-permissions.test.ts  (22 tests)
    admin route → privilege table
        - declares a privilege for every route on the admin router
        - has no entry pointing at a route the router does not serve
        - refuses a path that is not in the table
        - matches :param segments but not extra ones
    privilege evaluation
        - lets ADMIN and SUPERUSER through every section requirement
        - reserves the team routes for the superuser
        - treats MANAGE as including VIEW, but not the other way round
        - denies a section that is absent or explicitly NONE
        - keeps sections independent — Marketplace access grants nothing else
        - accepts any one of the sections on a shared route
        - lets any console account read the shared catalogs and its own profile
        - treats the pricing calculator as a read
        - explains the refusal in terms of the section
    PUT /config field scoping
        - maps a body to the sections it actually writes
        - lets a pricing operator write rates but not booking options
        - lets a booking-options operator write limits but not rates
        - requires both when the body straddles the two
        - needs MANAGE, not VIEW
        - ignores keys that are not settings — Zod rejects those moments later
    stored permissions
        - degrades a malformed column to no access rather than something unpredictable
        - keeps the sections it recognises and drops the rest
        - gives ADMIN and SUPERUSER the full map regardless of what is stored

apps/backend/src/__tests__/admin-section-access.middleware.test.ts  (8 tests)
    requireSectionAccess (mounted the way the app mounts it)
        - resolves paths relative to the /admin mount
        - refuses a section the operator does not hold
        - refuses a write when the operator only has view
        - allows the write once the operator has manage
        - scopes PUT /config by the keys in the body
        - lets a full admin through
        - turns away a deactivated console account
        - turns away a non-console role

apps/backend/src/__tests__/app-settings.service.test.ts  (30 tests)
    getBroadcastRadiusKm
        - returns the default (10) when the key is not seeded
        - parses the stored value
        - falls back to the default on a malformed value
    getRevealPhoneMinutes
        - returns the default (45) when the key is not seeded
        - parses the stored value
        - falls back to the default on a malformed value
    getSkillMatchingEnabled
        - defaults to on when the key is not seeded
        - reads a stored "false" as off
        - fails safe: anything other than "false" keeps matching on
    getPlatformConfig
        - falls back to defaults when nothing is seeded
        - reads the booking window from the DB when present
        - keeps a midnight end hour rather than treating 0 as unset
        - ignores an unparseable value and uses the default
        - includes matching/SLA defaults for unseeded keys
        - round-trips the extra-child fee type, which is a string not a number
        - reads a cleared fee type (empty string) as no fee at all
        - falls back to no fee on an unrecognised fee type
        - includes children defaults for unseeded keys
        - reads seeded matching/SLA values
        - defaults skill matching to on
        - reads a stored "false" for skill matching as a boolean, not a string
    updatePlatformConfig — coherence guard
        - accepts a coherent change
        - rejects a minimum longer than the maximum
        - catches an incoherent merge even when only one side is sent
        - rejects a window too short for the minimum booking
        - allows a cross-midnight window that comfortably fits the minimum
        - allows a full-24h window
        - rejects including more children at the base rate than are allowed at all
        - stores the skill-matching toggle as the text "false"
        - stores a cleared fee type as an empty string, not the text "null"

apps/backend/src/__tests__/auth-availability.test.ts  (8 tests)
    checkAvailability
        - reports both free
        - reports a taken email on its own
        - reports a taken phone on its own
        - reports both taken at once, so the client can flag both fields
        - looks the values up exactly as given — the route has already normalised them
    registerUser still refuses what checkAvailability reports as taken
        - 409s on a taken email, with the same message step 1 shows
        - 409s on a taken phone, with the same message step 1 shows
        - names the email first when both are taken

apps/backend/src/__tests__/auth-register-address.test.ts  (5 tests)
    registerUser — location
        - writes the wizard’s address as the user’s default Home row, in the same transaction
        - answers with the location read back off the new address row
    getMe — location
        - derives address, latitude and longitude from the default address row
        - reports no location for a user with no address
    updateProfile — location
        - never writes location columns, whatever an old client sends

apps/backend/src/__tests__/auth-register-nanny-profile.test.ts  (7 tests)
    registerUser — nanny profile population
        - sets the user avatar, populates the nanny profile, and reconciles certs + skills
        - falls back to empty catalog ids when none were chosen
        - does not create a nanny profile or reconcile anything for a mother
    registerUser — email verification token
        - spends the token inside the same transaction and marks the address verified
        - spends a mother’s token on the same terms — no account starts out unverified
        - does not create the user when the token is rejected
        - rejects either role’s payload with no token at the schema, before any service runs

apps/backend/src/__tests__/auth-service-id.test.ts  (3 tests)
    registerUser — ID verification defaults
        - starts a nanny at PENDING_REVIEW with the ID stored on the user row
        - starts a mother at PENDING_ID with no ID and no nanny profile
    submitId
        - moves the account to PENDING_REVIEW and clears the rejection reason

apps/backend/src/__tests__/booking-address.test.ts  (11 tests)
    createBooking — the chosen address
        - looks the address up as the mother’s own live row
        - 404s an address that is not hers, before pricing or inserting anything
        - writes the FK, the snapshot and the broadcast coordinates from the address
        - treats the address as part of the request’s identity when reusing a pending request
        - answers the mother with the full address
    the address on a booking read
        - the mother always sees the whole address
        - the nanny holding a %s booking sees the area only  [parameterised]
        - the nanny sees the whole address once the booking is %s  [parameterised]
        - the open-requests pool carries the area and nothing more
        - is null on a booking made before addresses existed
        - degrades a malformed snapshot to "no address" rather than failing the read

apps/backend/src/__tests__/booking-approval-flow.test.ts  (15 tests)
    VALID_TRANSITIONS
        - allows the pay-after-approval lifecycle
        - allows cancelling any non-terminal status
        - keeps legacy PENDING_CONFIRMATION transitions
        - treats COMPLETED, CANCELLED and REFUNDED as terminal
        - rejects skipping approval (PENDING → CONFIRMED)
    createBooking (broadcast)
        - creates an unassigned PENDING request priced at the fixed rate and broadcasts to eligible nannies + admins
    claim (unassigned request)
        - claims atomically: guarded updateMany assigns the nanny and moves PENDING → APPROVED, prompting the mother to pay
        - rejects the loser of a simultaneous claim race (updateMany matched no row)
        - rejects a second nanny once the request is already claimed (double-claim guard)
    nanny decision
        - acceptBooking sets nannyDecision ACCEPTED without changing status
        - declineBooking sets nannyDecision DECLINED without changing status
        - rejects a decision once the booking is no longer PENDING
    mockPayBooking
        - confirms an APPROVED booking on success and notifies the nanny
        - rejects payment for a booking that is not APPROVED
        - leaves the booking APPROVED when the payment fails

apps/backend/src/__tests__/booking-availability.test.ts  (15 tests)
    isTimeRangeWithinDaySchedule
        - accepts a range fully inside the window
        - accepts a range starting exactly on the window start (edge)
        - accepts a range ending exactly on the window end (edge)
        - rejects a range whose end overshoots the window end
        - rejects a range whose start is before the window start
        - rejects when the day is explicitly unavailable
        - rejects when the day key is missing entirely
        - honours minute-level window bounds (09:00–17:30)
        - rejects for a null or undefined schedule
        - exposes a human-readable out-of-window message
    doesDurationFitDaySchedule
        - true when start + duration stays within the window
        - false when start + duration overshoots the window
        - false on an unavailable day
    getDayScheduleWindowMinutes
        - returns the window length in minutes (honouring minute bounds)
        - returns null for unavailable, missing, or absent schedule

apps/backend/src/__tests__/booking-broadcast-radius.test.ts  (7 tests)
    notifyBookingBroadcast — radius filter
        - notifies only in-radius and coordinate-less nannies (plus admins)
        - notifies everyone when the booking has no coordinates
        - notifies everyone when the radius is 0 (filtering disabled)
        - offers the request to every approved, free nanny — approval is the only account gate
    listAvailableBookings — radius filter
        - shows only in-radius and coordinate-less requests to a located nanny
        - shows the full pool to a nanny without coordinates
        - shows the full pool when the radius is 0

apps/backend/src/__tests__/booking-broadcast-skills.test.ts  (13 tests)
    notifyBookingBroadcast — skill filter
        - notifies every nanny when the booking asks for no skills
        - notifies only nannies holding the requested skill
        - requires every requested skill, not just one of them
        - counts a fee-less skill as a requirement too
        - notifies nobody when no nanny holds the requested skills
    listAvailableBookings — skill filter
        - hides requests demanding a skill the nanny does not have
        - shows a request once the nanny holds every skill it asks for
        - shows the whole pool to a fully-skilled nanny
    acceptBooking — skill gate
        - refuses a claim from a nanny missing a required skill
        - allows a claim from a nanny holding every required skill
    skill matching switched off
        - broadcasts a skilled request to every nanny, skilled or not
        - shows the whole pool to a nanny with no skills
        - lets a nanny missing the skill claim the request

apps/backend/src/__tests__/booking-camera.service.test.ts  (22 tests)
    getBookingCamera
        - returns the stream and probe result for the booking parent
        - passes null through when the probe could not parse the URL
        - resolves the camera through the booking nanny, not the booking itself
        - picks the most recently added camera when a nanny has several
        - refuses a user who is not the booking parent
        - refuses the nanny on the booking — the feed is the parent’s view only
        - refuses access while the booking is %s  [parameterised]
        - never probes a camera the caller is not allowed to see
        - 404s when the booking does not exist
        - 404s when no nanny is assigned yet
        - 404s when the nanny has no camera set up
        - 401s for a deleted user
    notifyNannyToStartCamera
        - notifies the nanny and records the time
        - claims the cooldown conditionally so a double-tap cannot double-notify
        - rejects a second nudge inside the cooldown
        - allows another nudge once the cooldown has elapsed
        - tells the parent how long is left when it refuses
        - never reports a negative wait when the timestamp is stale
        - still succeeds when the nanny has no device token registered
        - refuses when the booking is not in progress
        - refuses a user who is not the booking parent
        - 404s when the nanny has no camera to turn on

apps/backend/src/__tests__/booking-create-children.test.ts  (9 tests)
    createBooking — children ceiling
        - rejects more children than one nanny is allowed to take
        - accepts exactly the maximum
    createBooking — pricing and snapshot
        - prices the extra children and snapshots who the booking is for
        - snapshots each child allergy alongside the name and age
        - charges nothing extra at or below the included allowance
    createBooking — saving children for next time
        - replaces the saved set when asked
        - leaves the saved set alone by default
        - writes the booking and the children in one transaction
    createBooking — idempotency
        - reuses a pending request only when the child count also matches

apps/backend/src/__tests__/booking-create-promo.test.ts  (6 tests)
    createBooking (promo wiring)
        - creates a booking with no discount when no promo code is provided
        - reserves a valid promo code on the booking without consuming it
        - rejects an invalid promo code and writes nothing
        - reuses an existing PENDING booking on retry without consuming the code
    validateBookingPromo
        - computes the discount from the priced subtotal (no fee added) and returns it
        - forbids non-mothers (403)

apps/backend/src/__tests__/booking-create-window.test.ts  (22 tests)
    createBooking — same-day booking
        - accepts a booking later today
    createBooking — minimum advance notice
        - rejects a start inside the lead time
        - accepts a start exactly on the lead-time boundary
        - rejects a booking in the past
        - with no lead time configured, still rejects the past but allows the imminent
    createBooking — daily window
        - rejects a start before the window opens
        - rejects an end past the window close
        - accepts a booking that crosses midnight when the window does
        - reports the window, not the lead time, when a booking breaks both
    createBooking — duration limits come from config
        - rejects a booking under the configured minimum
        - rejects a booking over the configured maximum
        - honours a raised minimum that the old hardcoded rule would have allowed
    createBooking — ordering and input shape
        - rejects an end before the start
        - rejects a zero-length booking
        - rejects an offset-bearing timestamp from an outdated client
    createBooking — what gets stored
        - converts wall-clock to the true UTC instant
        - applies the winter offset for a winter booking
        - derives date from the start, never from the client
        - dates an after-midnight booking to the day it starts, not the care-day it was picked from
        - returns times as platform-offset ISO strings
    createBooking — daylight saving
        - rejects a wall-clock time that does not exist on the spring-forward night
        - bills real elapsed hours across a DST change, not wall-clock hours

apps/backend/src/__tests__/booking-end-by-mother.test.ts  (9 tests)
    endBookingByMother
        - completes the booking and stamps motherEndedAt, not the nanny check-out
        - notifies the nanny that the parent ended the shift
        - still awards points and converts the referral, exactly like a nanny check-out
        - releases an extension still in flight so it cannot be paid after the shift
        - rejects a booking that is not under way
        - rejects an already-completed booking rather than double-awarding points
        - rejects a nanny trying to end the booking through the parent route
        - rejects a mother who does not own the booking
        - 404s on a missing booking

apps/backend/src/__tests__/booking-extension-window.test.ts  (15 tests)
    addHoursToWallClock
        - adds whole hours within a day
        - preserves the minutes of a non-hour-aligned time
        - rolls over midnight onto the next date
        - rolls back over midnight onto the previous date
        - crosses a month boundary
        - fails closed on malformed input
    extendablePresetHours — daily window (8 → 22)
        - offers every preset in the middle of the window
        - drops presets that would run past the window close
        - offers nothing once the booking already ends at the window close
        - caps on max duration before the window becomes the binding limit
        - offers nothing when the booking is already at the max duration
        - never offers an hour amount outside the presets
    extendablePresetHours — window across midnight (18 → 4)
        - extends an overnight booking up to the window close
        - stops at the window close on the far side of midnight
    extendablePresetHours — 24-hour window restricts nothing
        - is bounded only by the max duration

apps/backend/src/__tests__/booking-extension.service.test.ts  (22 tests)
    requestBookingExtension
        - creates a PENDING_NANNY quote priced off the booking’s frozen hourly rate
        - reserves no credits at request time — nothing is spent until the nanny agrees
        - notifies the nanny with the extension id she needs to answer
        - refuses hours that would run past the end of the daily booking window
        - refuses hours that would exceed the maximum booking duration
        - refuses a booking that is not under way
        - refuses a second request while one is still in flight
        - rejects a nanny using the parent route
    respondToBookingExtension
        - moves an accepted request to ACCEPTED and starts the payment clock
        - tells the mother the amount she now owes
        - declining settles the request and returns any reserved hours
        - refuses to answer a request that is no longer pending
        - rejects a nanny who is not on the booking
        - rejects a mother trying to accept on the nanny’s behalf
    applyPaidExtension
        - moves the end time out and folds the extension into the booking’s totals
        - marks the extension PAID with a timestamp
        - is a no-op on replay, so a repeated webhook cannot add the hours twice
        - refuses to apply an extension the nanny never accepted
        - leaves a booking that already ended untouched rather than reopening it
    cancelBookingExtension
        - withdraws the request and hands back reserved hours
        - refuses to withdraw an extension that is already paid for
        - rejects a mother who does not own the request

apps/backend/src/__tests__/booking-mother-id-gate.test.ts  (7 tests)
    createBooking — mother ID gate
        - blocks a mother who has never uploaded an ID (PENDING_ID)
        - blocks a mother whose ID was rejected (REJECTED)
        - allows a mother with an ID under review (PENDING_REVIEW) — upload-then-book
        - allows an APPROVED mother
    createBooking — mother email gate
        - blocks a mother who has not verified an email address
        - reports the email first when both gates are unsatisfied
        - allows a mother with a verified email

apps/backend/src/__tests__/booking-package-hours-integration.test.ts  (12 tests)
    createBooking — applying prepaid package hours
        - skips the transactional path entirely when the mother holds no hours
        - does not touch the balance when the mother opts out
        - credits the booking and leaves the nanny paid in full
        - asks the ledger for only the hours the booking needs
        - spends only the hours a promo-reduced total can pay for
        - prices the free-skill waiver off the bucket actually drawn from
        - records nothing when the ledger had nothing left to give
    cancelBooking — reversing prepaid package hours
        - returns the hours and undoes the credit on the booking
        - leaves a paid booking alone — the hours stay spent
        - does nothing when no hours were applied
        - applies BOTH reversals when a booking carries points and package hours
        - still cancels when the hours ledger fails

apps/backend/src/__tests__/booking-package-hours.service.test.ts  (16 tests)
    planPackageHoursRedemption
        - covers the whole booking when the balance and total allow it
        - spends only the hours a promo-reduced total can pay for
        - spends nothing when a 100% promo leaves nothing owed
        - never spends more hours than the parent holds
        - rounds affordable hours DOWN so the credit can never exceed the total
        - waives the most expensive skills first, up to the allowance
        - waives nothing when the package includes no free skills
        - scales by the duration multiplier so a discounted booking is not over-credited
        - spends nothing when there is no balance
        - spends nothing when an hour would be worth nothing
    resolvePackageHourValue
        - values an hour at base rate plus the waived surcharges only
        - drops to the bare base rate when the drawn bucket allows no free skills
        - applies the duration multiplier to the waived surcharges too
    packageHoursCreditFor
        - prices the hours that were actually debited, not the hours planned
        - is zero when nothing was applied
        - still caps at the total owed as a safety net

apps/backend/src/__tests__/booking-phone-reveal.test.ts  (7 tests)
    nannyPhoneIfRevealable
        - reveals the number once inside the window before start
        - reveals the number during the shift (IN_PROGRESS)
        - withholds the number before the window opens
        - withholds the number after the shift has ended
        - withholds the number for a not-yet-confirmed booking, even inside the window
        - returns null when the nanny has no phone on file
        - honors a custom (admin-configured) reveal window

apps/backend/src/__tests__/booking-shift.test.ts  (20 tests)
    booking shift transitions
        generateStartPin
            - returns a 4-digit PIN and persists only its hash (attempts reset)
            - rejects a non-mother caller
            - rejects a mother who does not own the booking
            - rejects when the booking is not CONFIRMED
            - rejects outside the check-in window (too early)
            - rejects while a balance-due adjustment is unpaid
        checkInBooking
            - transitions CONFIRMED to IN_PROGRESS with the correct PIN and clears it
            - rejects when the parent has not started (no PIN)
            - rejects an expired PIN
            - rejects a wrong PIN and increments the attempt counter
            - rejects once the attempt cap is reached
            - rejects check-in too early
            - rejects check-in when not CONFIRMED
            - rejects a valid PIN while a balance-due adjustment is unpaid
        checkOutBooking
            - transitions IN_PROGRESS to COMPLETED and notifies mother
            - carries an open balance-due on the booking response
            - leaves balanceDue null when nothing is owed
            - reads a pre-allergies child snapshot back with allergies null
            - rejects check-out from CONFIRMED
        - exports CHECK_IN_EARLY_MINUTES as 15

apps/backend/src/__tests__/booking-window.test.ts  (44 tests)
    bookingWindowLengthHours
        - measures a same-day window
        - measures a window that crosses midnight
        - treats an end at midnight as the close of the same day
        - treats equal bounds as a full 24 hours
    isBookingWithinDailyWindow — same-day window (8 → 22)
        - accepts a booking fully inside
        - accepts a booking starting exactly on the window open (edge)
        - accepts a booking ending exactly on the window close (edge)
        - rejects a booking starting before the window opens
        - rejects a booking running past the window close
        - rejects a booking that crosses midnight when the window does not
    isBookingWithinDailyWindow — cross-midnight window (8 → 2)
        - accepts a daytime booking
        - accepts a booking ending exactly at the post-midnight close (edge)
        - accepts a booking ending exactly at midnight
        - accepts an after-midnight booking as part of the previous care-day
        - rejects a booking starting after the post-midnight close
        - rejects a booking starting before the window opens
        - rejects a booking running past the post-midnight close
    isBookingWithinDailyWindow — full-24h window
        - accepts anything ordered when bounds are equal
        - still rejects an unordered range
    isBookingWithinDailyWindow — fails closed
        - rejects an end at or before the start
        - rejects malformed wall-clock input
        - rejects an impossible calendar date
        - rejects out-of-range window hours
    messages
        - describes a same-day window
        - describes a cross-midnight window
        - renders midnight and noon correctly
        - pluralises the lead time
        - does not say "at least 0 hours" when there is no lead time
    addDaysIso
        - advances across a month boundary
        - advances across a year boundary
        - handles a leap day
        - goes backwards
        - is unaffected by the Cairo DST transition
    careDayWallClock
        - keeps same-day hours on the care-day
        - rolls an hour past midnight onto the next date
        - never emits T24:00:00
    generateCareDaySlots
        - offers every start that fits the shortest booking (8 → 22, min 2)
        - carries the next day’s date on after-midnight slots (8 → 2, min 2)
        - sorts after-midnight slots last despite their low wall-clock hour
        - shrinks the offering as the minimum booking length grows
        - offers nothing when the window cannot fit the minimum
        - offers a full day when the bounds are equal
        - returns nothing for out-of-range window hours
        - every slot start is consistent with the window check

apps/backend/src/__tests__/campaign.schema.test.ts  (10 tests)
    CreateCampaignSchema — exactly-one-target
        - accepts a PACKAGE campaign with only packageId
        - accepts a PROMO_CODE campaign with only promoCodeId
        - rejects a PACKAGE campaign that also carries a promoCodeId
        - rejects a PACKAGE campaign with no packageId
        - rejects a PROMO_CODE campaign carrying packageId instead
        - rejects a non-URL imageUrl
        - rejects endsAt before startsAt
    UpdateCampaignSchema
        - requires at least one field
        - allows a lone sortOrder change
        - rejects switching to PACKAGE while supplying a promoCodeId

apps/backend/src/__tests__/campaign.service.test.ts  (12 tests)
    createCampaign
        - creates a PROMO_CODE campaign when the promo is active
        - throws notFound (404) when the promo target does not exist
        - throws badRequest (400) when the package target is inactive
    updateCampaign
        - throws notFound (404) when the campaign is missing
        - re-validates the target when target fields change
    deleteCampaign
        - soft-deletes an existing campaign
    listCampaigns
        - resolves targetName and targetUsageCount (promo → usageCount)
        - resolves package usage from grouped purchase counts
    listLiveCampaigns
        - maps a promo campaign to its code and drops dead-target rows
        - maps a package campaign to its packageId
    counters
        - recordImpression increments impressionCount
        - recordClick swallows a not-found update

apps/backend/src/__tests__/certification.service.test.ts  (15 tests)
    listCertifications
        - returns active-and-inactive certifications as DTOs, name-ordered
    listActiveCertifications
        - queries only active, non-deleted certifications and returns the public shape
    createCertification
        - creates a new certification when the name is free
        - throws conflict (409) when a live certification with the same name exists
        - allows reusing the name of a soft-deleted certification
    updateCertification
        - throws notFound (404) when the certification is missing
        - updates only provided fields
        - throws conflict (409) when renaming onto another live certification
    deleteCertification
        - throws notFound (404) when the certification is missing
        - soft-deletes and returns the id
    reconcileNannyCertifications
        - creates newly-added active certifications and validates them
        - soft-deletes certifications no longer wanted
        - reactivates a previously soft-deleted certification
        - keeps an already-held certification even when it is now inactive (no re-validation)
        - throws badRequest (400) when a newly-added id is invalid or inactive

apps/backend/src/__tests__/community.service.test.ts  (21 tests)
    community.service
        - forbids non-mothers from listing posts
        - creates a QA post
        - lists posts anonymously (guest) without a user lookup and with engagement flags false
        - gets a post anonymously (guest) with engagement flags false
        - lists comments anonymously (guest) without a like lookup
        - still forbids signed-in nannies from reading the feed
        - lists posts with pagination meta
        - toggles post like and updates count
        - rejects RSVP on non-event posts
    community.service — marketplace moderation
        - creates a marketplace listing as PENDING review
        - leaves QA posts approved — only listings are reviewed
        - scopes the feed to approved posts plus the signed-in author’s own
        - shows guests approved posts only
        - pins official listings to the top of the marketplace feed
        - keeps the mixed feed chronological
        - hides another member’s pending listing behind a 404
        - lets the author read her own rejected listing with the reason
        - sends an edited listing back to review and clears the rejection
        - does not re-review an edited QA post
        - refuses to let a member edit an official listing
        - returns the author’s own listings in every moderation state

apps/backend/src/__tests__/conversation.service.test.ts  (11 tests)
    conversation.service
        - forbids non-mothers from contacting seller
        - rejects contacting seller on own listing
        - hides a listing still awaiting review behind a 404
        - points buyers at the contact number on an official listing
        - returns existing conversation when buyer already contacted seller
        - creates a new marketplace conversation
        - lists conversations with pagination meta
        - sends a text message and updates conversation
        - notifies a mother recipient when a message is sent
        - does not notify a nanny recipient when a message is sent
        - returns total unread message count

apps/backend/src/__tests__/duration-rule.service.test.ts  (7 tests)
    listDurationRules
        - returns all non-deleted rules as DTOs, lowest tier first
    listActiveDurationRules
        - queries only active rules and returns the public shape
    createDurationRule
        - creates a tier when the minHours is free
        - throws conflict (409) when a tier for the same minHours exists
    updateDurationRule
        - throws notFound (404) when the rule is missing
        - updates provided fields
    deleteDurationRule
        - soft-deletes an existing rule

apps/backend/src/__tests__/email-render.test.ts  (9 tests)
    renderEmail RECEIPT
        - builds the subject and substitutes every variable
        - formats money with currency, two decimals and a thousands separator
        - leaves no unreplaced handlebars tokens
        - shows the discount line only when a discount was applied
    renderEmail EMAIL_VERIFICATION
        - renders the code, the expiry and the greeting inside the shared layout
        - keeps a leading zero — "004821" is a valid code, "4821" is not
        - keeps the code out of the subject, where a lock screen would show it
        - drops the greeting when there is no name yet (nanny sign-up)
        - leaves no unreplaced handlebars tokens

apps/backend/src/__tests__/email-transport.test.ts  (5 tests)
    - reports success when the recipient was accepted
    - reports failure when SMTP answered 250 but accepted nobody
    - logs the queue id and the From actually used, to tell a non-send from a dropped send
    - never logs the recipient address — email_logs already holds it
    - turns a thrown transport error into a result rather than propagating it

apps/backend/src/__tests__/email-verification.service.test.ts  (21 tests)
    sendEmailOtp
        - mails a 6-digit code, logs the send, and stores only its hash
        - lowercases the address so the same inbox rate-limits as one
        - refuses when no transport is configured, rather than silently doing nothing
        - refuses an address another account already holds
        - excludes the signed-in caller from the collision check, so they can re-verify their own address
        - sends anonymously during nanny sign-up, when no account exists yet
        - allows an immediate resend — the gap is the client timer to enforce now
        - caps sends per address per hour
        - does not create a row when the mail fails, so a lost code costs no allowance
    verifyEmailOtp
        - issues a single-use token for the right code and stores only its hash
        - rejects a wrong code and counts the attempt
        - burns the code once the attempt cap is reached
        - refuses a code past its cap without even comparing it
        - refuses an expired code
        - gives the same answer when no code is outstanding, so live addresses are not disclosed
    consumeVerificationToken
        - spends a live token
        - refuses a token already spent
        - refuses a token issued for a different address
        - refuses a token past its window
        - refuses an unknown token
        - looks the row up by hash, never by the token itself

apps/backend/src/__tests__/email.service.test.ts  (4 tests)
    sendReceiptEmail
        - sends the receipt to the paying parent and logs it as SENT
        - logs FAILED with the transport error and does not throw when sending fails
        - is a no-op with no send or log when email is not configured
        - swallows a DB failure while logging so payment capture is never blocked

apps/backend/src/__tests__/extra-child-fee.test.ts  (10 tests)
    resolveExtraChildFee
        - charges a flat fee per child beyond the allowance
        - charges per extra child, not once for the overage
        - reads a percentage fee against the base rate
        - charges nothing at or below the allowance
        - treats a null fee type as extra children being free
        - falls back to no fee rather than NaN on a malformed config
    calculatePriceBreakdown — with extra children
        - folds the fee into the hourly rate and scales it by the duration tier
        - splits the child fee with the nanny like every other component
        - stacks with skill add-ons rather than replacing them
        - prices a single-child booking exactly as before when no children are given

apps/backend/src/__tests__/geo.test.ts  (10 tests)
    distanceKm
        - is ~180 km between Cairo and Alexandria
        - is 0 for the same point
        - is symmetric
    toLatLng
        - returns null when either coordinate is missing
        - converts numeric and Decimal-like values
        - returns null for non-numeric values
    isWithinRadius
        - includes a point inside the radius
        - excludes a point outside the radius
        - always includes when either point is missing (fallback)
        - always includes when the radius is 0 (filtering disabled)

apps/backend/src/__tests__/nanny-approval-gate.test.ts  (3 tests)
    listNannies — who a parent can see
        - filters on admin approval and soft-deletes only
    requireApprovedNanny — who may use the nanny endpoints
        - lets an APPROVED nanny through
        - refuses a %s nanny with 403  [parameterised]

apps/backend/src/__tests__/nanny-booked-slots.test.ts  (7 tests)
    getNannyBookedSlots
        - labels slots with Cairo hours, not UTC hours
        - marks only the pre-midnight hours on the day a late booking starts
        - carries the post-midnight hours onto the following day
        - does not leak the previous day’s ordinary booking onto today
        - queries the requested day and the one before it
        - merges overlapping bookings without duplicating slots
        - returns nothing when the nanny has no bookings

apps/backend/src/__tests__/nanny-profile-update.test.ts  (5 tests)
    writeNannyProfileFields
        - routes user-level fields to the user row, profile fields to the upsert, and reconciles certifications
        - writes photo and date of birth onto the user row
        - clears the photo when avatarUrl is null
        - leaves the user row untouched when only profile fields are sent
        - reconciles certifications even when the array is empty, clearing all tags

apps/backend/src/__tests__/optional-auth.middleware.test.ts  (4 tests)
    optionalAuth
        - continues anonymously when no Authorization header is present
        - attaches the decoded token when a valid bearer token is sent
        - rejects an invalid or expired token with 401
        - rejects a malformed Authorization header with 401

apps/backend/src/__tests__/package-hours.service.test.ts  (31 tests)
    getAvailableHours
        - sums hoursRemaining across active non-expired buckets
        - queries only ACTIVE, non-deleted buckets with hours left, soonest-expiry first
        - excludes a bucket whose expiresAt has already passed
        - treats a null expiresAt as never-expiring
        - returns 0 when there are no active buckets
    getRedeemableSummary
        - reports spendable hours and the best free-skill allowance without debiting anything
        - reports a zero allowance when there is nothing to spend
    creditPurchaseHours
        - promotes PENDING_PAYMENT to ACTIVE, sets hoursRemaining and purchasedAt, keeps the stored expiresAt as-is, and writes a PURCHASE ledger row
        - is idempotent — calling it again on an already-ACTIVE purchase does not double-credit
        - reports SLOT_TAKEN and writes nothing when the user already has an active package
        - writes no ledger row when a concurrent capture won the promotion race
        - throws notFound (404) when the purchase does not exist
        - reports NO_PAYMENT and activates nothing when no captured payment exists
    redeemPackageHours (FIFO)
        - drains soonest-expiring bucket first and writes REDEMPTION rows
        - applies only what is available when short
        - debits atomically and guards against overdraw, then records the DB-read balance
        - skips a bucket a concurrent booking already drained, and reports the shortfall
        - returns zero applied hours and no purchaseIds when there is nothing to redeem from
    refundPackageHours
        - restores hours to the originating bucket and writes a REFUND row
        - sums refunds across multiple buckets that were drawn for the same booking
        - skips a bucket that has since expired
        - returns 0 when there is nothing to refund for that booking
        - is idempotent — a second call for the same bookingId is a no-op
    expirePackagesForUser
        - closes the bucket first, then forfeits whatever is actually left
        - forfeits only what survived a redemption that landed after the stale read
        - writes no EXPIRY row when a concurrent sweep already expired the bucket
        - does not write a ledger row when there are no hours left to forfeit
        - is a no-op when there are no stale buckets
    getMyPackageHours
        - resolves the user, lazily expires stale buckets, and returns the balance DTO
        - throws notFound (404) when there is no user for that firebaseUid
        - throws notFound (404) when the user has been soft-deleted

apps/backend/src/__tests__/package-payment.service.test.ts  (5 tests)
    purpose dispatch on capture
        - PACKAGE payment: credits hours and never touches the booking path
        - BOOKING payment: still confirms the booking exactly as before
    finalizePackagePaymentCaptured idempotency
        - credits hours once when the same capture is delivered twice
        - does nothing when the payment has no linked purchase
        - keeps the payment CAPTURED but does not roll back when the active slot is taken

apps/backend/src/__tests__/package-purchase.service.test.ts  (15 tests)
    listActivePackages
        - lists only active, offer-not-expired packages as public DTOs
        - queries only active, non-deleted packages whose offer is null-or-future, cheapest first
    createPackagePurchase
        - snapshots the package into a PENDING_PAYMENT purchase with computed expiry
        - queries the guard for an ACTIVE, non-deleted bucket with hours remaining and a null-or-future expiresAt
        - queries the guard for a PENDING_PAYMENT purchase created within the Paymob intention TTL
        - rejects (409) a second purchase while an active package still has hours
        - rejects (409) with a distinct message when a PENDING_PAYMENT checkout exists within the intention TTL
        - does NOT block when the existing ACTIVE bucket is fully consumed (hoursRemaining = 0)
        - does NOT block when the existing bucket is EXPIRED
        - does NOT block when the existing PENDING_PAYMENT purchase is older than the intention TTL (abandoned checkout)
        - does NOT block when the existing ACTIVE bucket has already passed its expiresAt
        - throws notFound (404) when there is no user for that firebaseUid
        - throws notFound (404) when the user has been soft-deleted
        - throws notFound (404) when the package does not exist, is inactive, or is soft-deleted
        - throws conflict (409) when the package catalog offer has already expired

apps/backend/src/__tests__/package.service.test.ts  (13 tests)
    listPackages
        - returns active-and-inactive packages as DTOs, name-ordered, price as number
    createPackage
        - creates a new package when the name is free
        - converts an ISO expiresAt string to a Date
        - throws conflict (409) when a live package with the same name exists
        - allows reusing the name of a soft-deleted package
    updatePackage
        - throws notFound (404) when the package is missing
        - updates only provided fields
        - sets validityDays without touching maxSkills when only validityDays is provided
        - sets maxSkills without touching validityDays when only maxSkills is provided
        - clears expiresAt when passed null
        - throws conflict (409) when renaming onto another live package
    deletePackage
        - throws notFound (404) when the package is missing
        - soft-deletes and returns the id

apps/backend/src/__tests__/payment-refund.service.test.ts  (4 tests)
    refundBookingPayment
        - rejects when no single captured payment can cover the amount
        - partial refund keeps the payment CAPTURED and increments refundedAmount
        - full refund flips the payment to REFUNDED
        - surfaces a conflict when the conditional update matches no rows (race)

apps/backend/src/__tests__/paymob-finalize-captured.test.ts  (3 tests)
    finalizePaymentCaptured (reached via reconcileStalePaymobPayments)
        - bookingId present: looks up the booking and confirms it as before
        - bookingId null: warns and returns without touching any booking
        - refuses to confirm a booking when no captured payment can be found

apps/backend/src/__tests__/paymob-hmac.test.ts  (4 tests)
    Paymob transaction HMAC
        - builds a deterministic plaintext string
        - verifies a correctly signed payload
        - rejects tampered HMAC
    Paymob merchant payment reference
        - normalizes retry suffixes back to the payment id

apps/backend/src/__tests__/paymob-intention-ttl.test.ts  (3 tests)
    createPaymobIntentionForBooking — link freshness
        - reuses the stored checkout link while it is still fresh
        - regenerates a fresh link once the stored one is older than the TTL
        - does not supersede or recreate when it resumes the fresh link

apps/backend/src/__tests__/paymob-refund-client.test.ts  (3 tests)
    paymob client refund
        - POSTs transaction_id + amount_cents and returns the parsed result
        - treats an explicit success:false as a failed refund (not an error)
        - throws AppError(502) on a non-OK response

apps/backend/src/__tests__/pin.test.ts  (2 tests)
    pin util
        - randomStartPin always returns 4 digits, including leading zeros
        - hashPin is deterministic and does not return the plaintext

apps/backend/src/__tests__/platform-time.test.ts  (14 tests)
    wallClockToUtc
        - reads a summer wall-clock as +03:00
        - reads a winter wall-clock as +02:00
        - rejects a wall-clock time that does not exist (spring forward)
        - accepts the hour either side of the spring-forward gap
        - resolves an ambiguous fall-back wall-clock to the later instant
        - rejects a malformed wall-clock
    toPlatformIso
        - renders a summer instant with the +03:00 offset
        - renders a winter instant with the +02:00 offset
        - round-trips with wallClockToUtc
    toPlatformDateIso
        - uses the platform calendar day, not the UTC one
        - agrees with UTC when the day does not straddle
    toPlatformWallClock
        - drops the offset
    platformHour
        - returns the Cairo hour, not the UTC hour
    toPlatformDateColumn
        - returns midnight UTC of the platform date, as @db.Date expects

apps/backend/src/__tests__/pricing-config.service.test.ts  (6 tests)
    getPricingInputs / getPricingConfig
        - composes base rate, split, add-on skills and duration tiers
    buildBreakdown
        - prices selected add-ons with the matching duration tier and split
        - rejects an unknown skill id
        - applies the configured extra-child fee to the hourly rate
        - defaults to one child when the caller has no children context
    previewBreakdown
        - loads inputs and returns a full breakdown for the admin calculator

apps/backend/src/__tests__/pricing.service.test.ts  (10 tests)
    resolveEffectiveRate
        - adds flat and percentage skill fees on top of the base rate
        - treats a null-fee skill as a zero surcharge
        - returns the base rate unchanged with no add-ons
    resolveDurationMultiplier
        - picks the highest tier at or below the booking length
        - applies a tier exactly at its minHours boundary
        - defaults to 1 when no tier matches
    calculatePriceBreakdown
        - prices base rate only with the nanny/platform split
        - applies skill add-ons and a duration multiplier before the split
        - subtracts a promo discount from the subtotal before splitting
        - caps the discount at the subtotal so totalAmount never goes negative

apps/backend/src/__tests__/promo-code.service.test.ts  (14 tests)
    validatePromoCode
        - throws notFound (404) when the code does not exist
        - throws badRequest (400) when inactive
        - throws badRequest (400) when expired
        - throws badRequest (400) when maxUsage is reached
        - throws badRequest (400) when the per-user limit is reached
        - returns the flat value for a FLAT code
        - returns applicableAmount * value / 100 for a PERCENTAGE code
        - caps the discount at the applicable amount
    redeemPromoCode
        - increments usageCount and writes a redemption row
    redeemBookingPromoCodeOnCapture
        - consumes the code the booking reserved
        - does nothing when the booking reserved no code
        - is idempotent when a redemption already exists for the booking
    validatePromoCode usage caps
        - counts bookings holding the code unpaid against maxUsagePerUser
        - counts bookings holding the code unpaid against maxUsage

apps/backend/src/__tests__/qa-checklist.service.test.ts  (12 tests)
    getQaChecklist
        - is empty before anyone has run anything
        - reads only the checklist rows, not the rest of app_settings
        - keys entries by scenario id, with the prefix stripped
        - drops a row whose scenario has been retired from the catalogue
        - treats an unparseable row as never-run rather than failing the whole checklist
        - treats a row whose JSON no longer matches the shape as never-run
    setQaScenarioStatus
        - writes the entry under its namespaced key
        - defaults the free-text fields rather than storing undefined
        - revives a soft-deleted row instead of leaving the write invisible
        - refuses a scenario id that is not in the catalogue, and writes nothing
        - refuses an id that tries to smuggle the prefix in
    resetQaChecklist
        - deletes only the checklist rows

apps/backend/src/__tests__/reapply-package-hours.service.test.ts  (2 tests)
    reapplyPackageHoursForBooking
        - revives the existing REDEMPTION row instead of inserting a duplicate, and writes no REFUND
        - releases the reservation (soft-deletes the row) when apply is false

apps/backend/src/__tests__/referral.service.test.ts  (25 tests)
    getOrCreateReferralCode
        - returns the existing code without generating a new one
        - generates a code from the first name on first use
        - falls back to a generic stem when the name has no Latin letters
        - retries with a fresh suffix when the generated code collides
        - returns the winner when a concurrent request set the code first
    applyReferralCode
        - links the referral and credits the invitee their welcome points
        - accepts a lowercase, untrimmed code
        - rejects self-referral
        - rejects an unknown code with the same message as self-referral
        - rejects a caller who has already used a code
        - rejects a caller who already completed a booking
        - rejects a non-parent caller
        - rejects when the program is switched off
        - turns a racing duplicate insert into a friendly conflict
        - still links the referral when the welcome grant is configured to zero
    convertReferralForBooking
        - credits the referrer and marks the referral converted
        - does not double-credit when a concurrent call already converted it
        - is a no-op for a parent who was never referred
        - is a no-op when the program is switched off
        - snapshots the payout so later config changes do not rewrite history
    getReferralSummary
        - counts only converted referrals as joined and as earned points
    validateReferralCode
        - confirms a valid code and names the referrer
        - validates for an anonymous caller mid-signup, before an account exists
        - reports a self-referral as simply invalid
        - reports invalid without a lookup when the program is off

apps/backend/src/__tests__/reward.service.test.ts  (21 tests)
    getRewardConfig
        - returns program defaults when no config row exists
        - returns the stored config when a row exists
    updateRewardConfig
        - creates the singleton on first save
        - updates the existing singleton
    awardPointsForBooking
        - awards round(hours * rate) points and notifies the parent
        - is idempotent — no-op when an EARN entry already exists for the booking
        - does nothing when the program is disabled
    grantPoints
        - credits points and records an ADMIN_GRANT entry
        - clamps a revoke so the balance never goes negative
        - throws 400 when revoking from a zero balance
        - throws 404 when the target user does not exist
    applyBookingRedemption
        - deducts points, records a REDEEM entry, and returns the discount
        - caps redeemed hours at the booking duration
        - throws 400 when the balance is insufficient
        - throws 400 when below the minimum redemption
        - throws 400 when the program is disabled
    refundBookingRedemption
        - restores the points and records a REFUND entry
        - is a no-op when there are no points to refund
    listWallets (paginated)
        - applies skip/take for the page and returns the wallet DTOs + meta
        - passes a case-insensitive name/email search into the where clause
        - defaults a wallet-less parent to zeroed balances

apps/backend/src/__tests__/shared-package-schemas.test.ts  (6 tests)
    package shared schemas
        - requires validityDays >= 1 and maxSkills >= 0 on create
        - PublicPackageSchema omits internal fields but keeps validity + maxSkills
        - PackageHoursBalanceSchema shape
    admin package purchase schemas
        - parses a purchase list row with buyer + consumed hours
        - list query coerces page/limit and accepts a status filter
        - detail carries a ledger array with signed hours + balanceAfter

apps/backend/src/__tests__/skill.service.test.ts  (10 tests)
    listSkills
        - returns active-and-inactive skills as DTOs, name-ordered
    listActiveSkills
        - queries only active, non-deleted skills and returns the public shape
    createSkill
        - creates a new skill when the name is free
        - throws conflict (409) when a live skill with the same name exists
        - allows reusing the name of a soft-deleted skill
    updateSkill
        - throws notFound (404) when the skill is missing
        - updates only provided fields
        - throws conflict (409) when renaming onto another live skill
    deleteSkill
        - throws notFound (404) when the skill is missing
        - soft-deletes and returns the id

apps/backend/src/__tests__/storage.test.ts  (4 tests)
    objectPathFromDownloadUrl
        - extracts and URL-decodes a nested nanny-ids object path
        - handles a path with no query string
        - returns null for a URL without an /o/ segment
        - returns null for junk input

apps/backend/src/__tests__/stream-probe.test.ts  (7 tests)
    probeStream
        - reports online when the camera accepts a connection
        - reports offline when nothing is listening on the port
        - reports null — not offline — when the URL cannot be parsed
        - defaults to the standard RTSP port when the URL omits one
        - reuses a cached result instead of redialling the camera
        - does not serve a cached result after the camera is repointed
        - probes again once the cache has been cleared

apps/backend/src/__tests__/support-contact.schema.test.ts  (18 tests)
    normalizePhone
        - strips spaces and dashes but keeps a leading +
        - leaves an already-normalized number alone
        - trims surrounding whitespace and returns empty for a blank value
        - normalizes a leading 00 international-access prefix to +
        - strips parentheses and dots from a pasted number
        - strips parentheses when normalizing a 00-prefixed number
    whatsappLink
        - builds a wa.me URL from digits only, dropping the + and separators
    SupportContactSchema
        - accepts a fully configured contact
        - accepts empty strings so an admin can retire a channel
        - rejects a number with too few digits
        - rejects a number with letters in it
        - rejects a local-format number missing the country code
        - accepts a number with a 00 international-access prefix, normalized to +
        - accepts a number with parentheses and dots
        - accepts a number already in canonical + form
        - rejects a malformed email
    UpdateSupportContactSchema
        - accepts a partial payload touching one channel
        - still validates the fields that are present

apps/backend/src/__tests__/support-contact.service.test.ts  (9 tests)
    getSupportContact
        - returns empty strings when nothing is seeded, so no card renders
        - returns seeded values
        - filters out soft-deleted rows
    updateSupportContact
        - writes only the fields provided
        - normalizes a number before storing it
        - trims but does not strip dashes from an email
        - accepts an empty string, blanking a channel
        - ignores undefined fields rather than blanking them
        - writes atomically and returns the full resulting contact

apps/backend/src/__tests__/support-faq.service.test.ts  (5 tests)
    getSupportFaq
        - answers with the built-in questions until an operator has written any
        - returns what the operator saved, in the order it was saved
        - treats a row that will not parse as unset rather than crashing the screen
    updateSupportFaq
        - stores the list as one JSON row and reads it back
        - lets an operator clear the list entirely

apps/backend/src/__tests__/vercel-bundle-assets.test.ts  (3 tests)
    vercel bundle packaging
        - emits the serverless bundle
        - ships every email template where the bundled renderer looks for it
        - ships the templates byte-for-byte, not a stale copy

================================================================================================
BACKEND — integration tests (Jest, real PostGIS + Auth emulator + Paymob fake)
154 tests in 26 files
================================================================================================

apps/backend/src/__integration__/harness.smoke.test.ts  (6 tests)
    integration harness
        - serves an unauthenticated health check
        - rejects a request with no bearer token
        - accepts an emulator-issued token through the real auth middleware
        - persists factory rows to the real database
        - starts each test from an empty database
        - restores the seeded platform settings after each reset

apps/backend/src/__integration__/journeys/a01-booking-lifecycle.test.ts  (1 test)
    A1 — booking lifecycle
        - runs broadcast → claim → approve → pay → care → review

apps/backend/src/__integration__/journeys/a02-admin-reject.test.ts  (2 tests)
    A2 — admin rejects a pending booking
        - cancels the request, records the reason, and never touches money
        - refuses to reject a booking that is already terminal

apps/backend/src/__integration__/journeys/a03-refund.test.ts  (5 tests)
    A3 — refund a paid booking
        - shortening a paid booking leaves a refundable overpayment, not a charge
        - refunds the overpayment to the card through the real Paymob path
        - refuses a second refund once the overpayment is exhausted
        - refuses to refund more than was overpaid
        - can settle the overpayment as Care Points instead of money

apps/backend/src/__integration__/journeys/a04-promo-code.test.ts  (8 tests)
    A4 — promo codes
        - previews a percentage discount against a subtotal
        - never discounts more than the amount owed
        - charges the discounted total and consumes the code only on payment
        - counts an unpaid booking against the usage cap while it holds the code
        - refuses an expired code
        - refuses a deactivated code
        - refuses an unknown code
        - enforces a per-user cap across separate paid bookings

apps/backend/src/__integration__/journeys/a05-care-points.test.ts  (7 tests)
    A5 — Care Points
        - credits an admin grant to the wallet and its ledger
        - redeems points for care hours and charges only the remainder
        - returns the points when the redemption is undone
        - refuses to spend more points than the wallet holds
        - refuses a second redemption while one is already applied
        - returns points to the wallet when a booking carrying them is cancelled
        - refuses to redeem against a booking that is not yet approved

apps/backend/src/__integration__/journeys/a06-package-hours.test.ts  (9 tests)
    A6 — package hours
        - credits hours only once the purchase is paid
        - draws hours down when a booking uses them
        - lets a mother save her hours and pay cash instead
        - covers what it can and charges for the rest
        - returns the hours when the booking is cancelled
        - reconciles a purchase whose webhook never arrived
        - shows the purchase in the admin console
        - refuses to buy an inactive package
        - refuses a purchase whose body and path disagree

apps/backend/src/__integration__/journeys/a07-extension.test.ts  (6 tests)
    A7 — mid-care extension
        - runs request → accept → pay and moves the end time
        - leaves the booking alone when the nanny declines
        - lets the parent withdraw a request before it is answered
        - allows only one open request per booking
        - refuses to extend a booking that is not under way
        - refuses an unsupported extension length

apps/backend/src/__integration__/journeys/a08-time-edit-adjustment.test.ts  (4 tests)
    A8 — admin edits booking times
        - previews the delta without changing anything
        - lengthening a paid booking raises a balance due the mother can settle
        - shortening a paid booking refunds rather than charges
        - rejects a commit carrying a stale revision

apps/backend/src/__integration__/journeys/a09-webhook-resilience.test.ts  (5 tests)
    A9 — payment webhook resilience
        - reconciles a dropped webhook through the sync endpoint
        - treats a replayed webhook as a no-op
        - rejects a forged signature and leaves the booking unpaid
        - rejects a webhook carrying no signature at all
        - records a declined payment without confirming the booking

apps/backend/src/__integration__/journeys/a10-nanny-onboarding.test.ts  (9 tests)
    A10 — nanny onboarding and approval
        - registers a nanny awaiting review, invisible to parents
        - tells step 1 of the wizard when a phone or email is already taken
        - is not broadcast a booking request while unapproved
        - currently lets an unvetted nanny claim a booking she was never offered
        - currently lets an explicitly rejected nanny claim one too
        - appears in the admin nannies queue and becomes discoverable on approval
        - can claim and be paid for a booking once approved
        - keeps a rejected nanny out of search and tells her why
        - refuses to approve the same nanny twice

apps/backend/src/__integration__/journeys/a11-mother-id-gate.test.ts  (6 tests)
    A11 — mother ID verification gates booking
        - registers a mother at PENDING_ID
        - refuses a booking from a mother who has never uploaded an ID
        - lets her book as soon as the ID is submitted, before review
        - opens booking the moment an admin approves
        - closes booking again if the ID is rejected
        - lets a rejected mother resubmit and be approved

apps/backend/src/__integration__/journeys/a12-operator-access-matrix.test.ts  (8 tests)
    A12 — operator access matrix
        - declares a privilege for every admin route
        operator with %s access  [parameterised]
            - is refused exactly the routes the permission table refuses
        - refuses superuser-only routes to an operator holding every section
        - lets a superuser through the same route
        - gates PUT /config by which keys the body writes
        - refuses an operator whose stored permissions are malformed
        - still refuses a section an operator was never granted
        - refuses a MANAGE route to a VIEW-only operator on that section

apps/backend/src/__integration__/journeys/a13-hosted-checkout.test.ts  (5 tests)
    A13 — hosted checkout loop
        - serves a checkout page for the intention the app opens
        - 404s a checkout for an unknown client secret
        - pays a booking end to end without the test posting the webhook
        - leaves the booking unpaid when the customer declines
        - serves the return page the WebView lands on

apps/backend/src/__integration__/journeys/a14-mother-email-gate.test.ts  (12 tests)
    A14 — the address is proven at registration
        - creates the account with the proven address, not the phone placeholder
        - refuses to register a mother who brings no token
        - refuses a token that was issued for a different address
        - lets her book straight away — nothing gates on email any more
    A14 — accounts created before the rule
        - refuses a booking from an account that never proved an address
        - mails a code, swaps it for a token, and opens booking once the address is attached
        - records the send in the email log
        - rejects a wrong code and leaves the account unverified
        - refuses to spend a verification token twice
        - refuses a token that was issued for a different address
        - refuses an address another live account already holds
        - caps sends to one address at five an hour, with no cooldown between them

apps/backend/src/__integration__/journeys/a15-broadcast-matching.test.ts  (6 tests)
    A15 — broadcast radius
        - reaches a nanny inside the radius and skips one outside it
        - reads the radius from the platform settings, not a constant
        - never hides work from a nanny whose profile has no coordinates
    A15 — skill matching
        - offers a request priced with a skill add-on only to nannies who hold it
        - refuses a direct claim from a nanny missing the skill, and leaves the request open
        - stops filtering on skills when the admin switches matching off

apps/backend/src/__integration__/journeys/a16-claim-race.test.ts  (4 tests)
    A16 — first to accept wins
        - refuses a second nanny once the request is claimed, and keeps the first
        - lets exactly one of two simultaneous accepts through
    A16 — decline
        - is refused on an unclaimed request, which stays open to everyone else
        - is recorded on a booking assigned to her without moving its status

apps/backend/src/__integration__/journeys/a17-cancellation.test.ts  (14 tests)
    A17 — who may cancel
        - lets the mother withdraw an unclaimed request, which leaves every nanny’s pool
        - refuses anyone who is not a party to the booking
        - refuses to cancel a booking that has already completed
        - refuses to cancel a shift that is under way — the parent ends it instead
        - requires a reason
    A17 — the other party is told
        - tells the nanny when the mother cancels
        - tells the mother when the nanny cancels
        - tells nobody when an unclaimed request is withdrawn
    A17 — the refund quote
        - is the full amount when the mother cancels more than 24 hours ahead
        - is half when the mother cancels inside 24 hours
        - is the full amount when the nanny cancels, however close to the start
        - uses the window the console configures, not a constant
        - is always the full amount when the window is zero
        - is published to the app on /bookings/options so the warning matches the charge

apps/backend/src/__integration__/journeys/a18-end-early.test.ts  (4 tests)
    A18 — ending a shift early
        - completes the booking, records that the parent ended it, and tells the nanny
        - is the parent’s action alone
        - refuses a shift that has not started
        - cannot be ended twice, nor checked out after it has been ended

apps/backend/src/__integration__/journeys/a19-nanny-rating.test.ts  (3 tests)
    A19 — the rating average
        - counts every review and averages them
        - takes one review per booking
        - refuses a review before the booking has completed, and from anyone but its mother

apps/backend/src/__integration__/journeys/a20-profiles.test.ts  (6 tests)
    A20 — a mother edits her account
        - changes her name, photo and address, and the console sees the same record
        - adds and removes children, and the saved list is what the booking form offers
        - refuses a blank name rather than saving it
    A20 — an operator edits a nanny
        - changes her bio and experience, and her own Profile tab shows the change
        - a skill the operator removes stops her being offered requests that need it
        - is not something the nanny can do herself

apps/backend/src/__integration__/journeys/a21-addresses.test.ts  (7 tests)
    A21 — a mother manages her address book
        - starts with the address she registered with, as her default
        - adds, edits, promotes and deletes addresses, keeping exactly one default
        - cannot reach another mother’s address
        - a nanny can read her address but only support can change it
    A21 — the address a booking is made at
        - books at the chosen address, snapshots it, and refuses an address that is not hers
        - shows the nanny only the area until the booking is paid for
    A21 — an operator moves a nanny
        - rewrites her single address, and proximity search and her profile follow

apps/backend/src/__integration__/journeys/a21-cameras.test.ts  (7 tests)
    A21 — the camera catalogue
        - lists a camera against the nanny it is assigned to, and only ID-verified nannies are offered
        - is behind the Cameras section
    A21 — the live monitor
        - opens for the mother during the shift, and the booking says a camera exists
        - is not offered before the shift starts, nor to the nanny
        - reports "no camera" for a nanny without one, and follows a reassignment
    A21 — asking the nanny to switch the camera on
        - notifies the nanny once, then holds the parent to the cooldown
        - cannot be sent by the nanny, or for a booking that is not running

apps/backend/src/__integration__/journeys/a22-support-faq.test.ts  (4 tests)
    A22 — support FAQ
        - answers the built-in questions before an operator has written any
        - shows the app exactly what the operator saved, in that order
        - refuses an entry with no answer, and keeps what was there
        - is behind the Settings section, and writing needs MANAGE

apps/backend/src/__integration__/journeys/toolkit.smoke.test.ts  (1 test)
    journey toolkit
        - drives a booking through the full lifecycle

apps/backend/src/__integration__/paymob-fake.smoke.test.ts  (5 tests)
    paymob fake
        - answers the real API client with a usable intention
        - signs a webhook the production verifier accepts
        - rejects a tampered signature
        - reports the merchant reference the intention was created with
        - refunds against the captured transaction

================================================================================================
SHARED PACKAGE — schema tests (Vitest)
43 tests in 7 files
================================================================================================

packages/shared/src/__tests__/address.test.ts  (13 tests)
    parseAddressComponents
        - reads governorate, area and a numbered street off a street_address result
        - leaves the street null when the pin only resolved to a plus code
        - uses the route alone when there is no street number
        - returns all nulls for an empty or missing component list
    formatAddressArea
        - joins area and governorate, the line a nanny judges distance by
        - drops a missing half rather than printing a dangling comma
        - falls back to the formatted line for a backfilled row with no parts
    AddressInputSchema
        - accepts the minimum a map pick produces and nulls the optional parts
        - trims every text field and turns an emptied one into null
        - refuses an address with no label or no coordinates
    UpdateAddressSchema
        - leaves an unsent part untouched instead of clearing it
        - still lets a part be cleared explicitly
    BookingAddressSchema
        - parses a row the migration backfilled (numeric strings, null parts)

packages/shared/src/__tests__/admin-nanny-update.test.ts  (4 tests)
    UpdateAdminNannySchema
        - accepts the fields an admin can now correct: photo, date of birth and home pin
        - lets the photo be cleared with null
        - does not carry the pin or the address line — those go through PUT /nannies/:id/address
        - refuses a malformed date of birth

packages/shared/src/__tests__/auth-availability.test.ts  (4 tests)
    CheckAvailabilitySchema
        - normalises the email the same way every other auth body does
        - refuses a phone that is not E.164
        - uses the one E.164 rule the register body uses too
    AvailabilityResponseSchema
        - is two booleans

packages/shared/src/__tests__/booking-address.test.ts  (8 tests)
    CreateBookingSchema
        - requires the address the mother chose
        - no longer carries loose coordinates — the address is the location
    UpdateProfileRequestSchema
        - drops address and coordinates — those are edited through /addresses
    admin schemas
        - edits a booking location by choosing one of the mother addresses, not loose coordinates
        - no longer takes a free-text nanny location on the profile patch
        - upserts a nanny address without a label or default flag — she has exactly one
        - carries the address book on the detail pages
    UpdateNannyProfileRequestSchema
        - drops location — a nanny address is set at registration and edited by admins

packages/shared/src/__tests__/harness.smoke.test.ts  (2 tests)
    shared schema harness
        - parses a valid payload
        - rejects an invalid field and reports its path

packages/shared/src/__tests__/qa-scenarios.test.ts  (7 tests)
    QA scenario catalogue
        - has a unique id for every scenario
        - gives every scenario a title, at least one step and at least one expectation
        - uses kebab-case ids, so a persistence key never needs escaping
        - recognises its own ids and rejects anything else
        - leads with the flows that happen most often
        - caps the free-text fields a public endpoint accepts
        - rejects a status outside the enum

packages/shared/src/__tests__/register-nanny-required.test.ts  (5 tests)
    RegisterRequestSchema — what a nanny must provide
        - accepts a complete nanny
        - needs a street address
        - needs at least one age range
        - needs at least one working day
        - asks none of this of a mother

================================================================================================
ADMIN WEB — unit / component tests (Vitest + Testing Library + MSW)
27 tests in 7 files
================================================================================================

apps/admin/src/features/addresses/__tests__/address-editor.test.tsx  (3 tests)
    AddressEditor (no maps key)
        - offers typed coordinates instead of a map, prefilled from the address
        - refuses to save without an address line or coordinates
        - saves the parsed body — parts, landmark and numeric coordinates

apps/admin/src/features/id-reviews/__tests__/id-review-card.test.tsx  (2 tests)
    IdReviewCard
        - approves through the mother endpoint
        - offers no decision on an already-approved ID

apps/admin/src/features/nannies/__tests__/nanny-profile-editor.test.tsx  (2 tests)
    NannyProfileEditor
        - sends photo and date of birth
        - clears the photo with null

apps/admin/src/features/qa/__tests__/qa-checklist-page.test.tsx  (10 tests)
    QaChecklistPage
        - renders the catalogue with no signed-in user
        - numbers scenarios by their position in the catalogue
        - counts nothing as run before anyone has recorded a result
        - reflects results the server already holds
        - saves a result to the server when one is picked
        - puts the previous result back when the save fails
        - carries a half-typed note into the write that records the result
        - shows the steps and the expected outcome when a scenario is opened
        - narrows the list when a filter is applied
        - explains itself when the checklist cannot be loaded

apps/admin/src/pages/__tests__/dashboard-page.test.tsx  (1 test)
    DashboardPage
        - derives every stat card from the rows the list pages show

apps/admin/src/pages/__tests__/settings-page.test.tsx  (7 tests)
    SettingsPage — skill matching toggle
        - reads the saved state back from the config
        - flips the live preview before anything is saved
        - saves the toggle as a boolean alongside the numeric settings
    SettingsPage — FAQ editor
        - reads the saved questions back, in order
        - saves the whole list after a question is added
        - saves the list without a removed question
        - refuses to save an entry with no answer, and says which one

apps/admin/src/test/harness.smoke.test.tsx  (2 tests)
    admin test harness
        - renders data fetched through apiClient and MSW
        - lets a test override a handler to exercise the failure path

================================================================================================
ADMIN WEB — end-to-end tests (Playwright, real browser + backend)
68 tests in 13 files
================================================================================================

apps/admin/e2e/a01-approve-booking.spec.ts  (5 tests)
    - shows a new request in the pending queue
    - opens a booking by clicking its row
    - follows a claimed and paid booking through the status filters
    - shows the money and both parties on the detail page
    - does not offer Approve on an unclaimed request

apps/admin/e2e/a02-reject-booking.spec.ts  (4 tests)
    - rejects a pending request with a reason
    - shows the rejection reason on the detail page
    - leaves the booking alone when the dialog is dismissed
    - offers no actions on a booking that is already terminal

apps/admin/e2e/a03-refund.spec.ts  (5 tests)
    - previews a shortened booking as a refund, before saving anything
    - saves a shortened booking and refunds the overpayment to the card
    - records the refund on the detail page
    - can return an overpayment as Care Points instead of money
    - asks for confirmation before charging the mother more

apps/admin/e2e/a12-operator-ui.spec.ts  (8 tests)
    operator with bookings:MANAGE and users:VIEW
        - shows only the granted sections in the sidebar
        - redirects a hand-typed URL for a forbidden section
        - renders a MANAGE section with its write controls
        - renders a VIEW section read-only
        - cannot reach the superuser-only team page
    operator with a single section
        - lands on its only section rather than the dashboard
        - hides even the sections a wider operator can see
    superuser
        - sees every section and the team page

apps/admin/e2e/b01-manage-operators.spec.ts  (5 tests)
    - a created operator can sign in, and reaches only what was granted
    - widening a grant reaches the operator on their next sign-in
    - narrowing a grant closes the section it removed
    - removing an operator ends their access at the door
    - the superuser cannot remove or re-scope itself here

apps/admin/e2e/b02-session-lifecycle.spec.ts  (7 tests)
    signed out
        - remembers where you were going and returns you there
        - sends a signed-in visitor on from the login page
    signed in as superuser
        - attaches a bearer token to every backend call
        - refreshes and replays a request the server rejects once
        - gives up rather than looping when the refresh does not help
    signing out
        - ends the session and does not leave it behind
        - a protected page is closed again afterwards

apps/admin/e2e/b03-catalogue-downstream.spec.ts  (5 tests)
    - a new skill reaches the catalogue a nanny picks from
    - a new certification reaches the nanny picker
    - a new package is offered to a mother
    - a new promo code discounts a real checkout
    - a booking rule changed in the console reaches the app

apps/admin/e2e/b04-bookings-console.spec.ts  (6 tests)
    - overrides a booking status straight from the queue
    - locks the override once a booking is completed
    - offers only the transitions the server will accept
    - pages through a queue longer than one screen
    - changing the filter returns to the first page
    view-only operator
        - is not offered the status override it cannot use

apps/admin/e2e/b05-users-and-id-review.spec.ts  (9 tests)
    - lists a newly registered parent awaiting review
    - approving an ID clears it from the queue and lands on the account
    - rejecting an ID records the reason the user will be shown
    - a rejected ID is findable again under its own filter
    - the gallery is for parents — a waiting nanny is not offered there
    - the Mommies tab lists a parent and follows her status
    - a decision in the gallery shows up on the per-role tab
    - a nanny is approved from her own page, and the Nannies queue lets her go
    - rejecting a nanny records the reason she will be shown

apps/admin/e2e/b06-marketplace-moderation.spec.ts  (7 tests)
    - a new listing waits for review and reaches nobody meanwhile
    - approving publishes it and opens the seller’s inbox
    - rejecting sends the seller the reason
    - a rejected listing the seller fixes comes back to the queue
    - taking down a live listing pulls it out of the feed
    - editing a live listing sends it back through review
    - an official listing is published rather than reviewed

apps/admin/e2e/b07-support-faq.spec.ts  (2 tests)
    - a question added in the console reaches the app, in the console’s order
    - an entry with no answer is refused before anything is sent

apps/admin/e2e/b08-nanny-address.spec.ts  (2 tests)
    - rewrites her address and the console and the app both read the new one
    - refuses to save an address with no coordinates

apps/admin/e2e/harness.smoke.spec.ts  (3 tests)
    signed in as superuser
        - restores the saved session instead of redirecting to login
        - loads authenticated data from the backend
    signed out
        - redirects an anonymous visitor to the login page

================================================================================================
MOBILE APP — unit / component tests (Jest + React Native Testing Library)
173 tests in 38 files
================================================================================================

apps/mobile/src/__tests__/harness.smoke.test.tsx  (5 tests)
    mobile test harness
        - renders a component tree
        - renders react-native-maps through the global stub
        - resolves location permissions and a position
        - resolves notification permissions and a push token
        - defaults the image picker to a cancelled selection

apps/mobile/src/components/__tests__/AddressForm.test.tsx  (5 tests)
    AddressForm
        - refuses to save until the address has been searched for or pinned
        - fills the parts from a search pick and submits them with the Home label
        - reverse-geocodes a dropped pin into the line and parts, keeping typed fields
        - asks for a name when the label is "Other"
        - prefills every field from an existing address and keeps its id out of the payload

apps/mobile/src/components/__tests__/BookingLocationSection.test.tsx  (4 tests)
    BookingLocationSection
        - lists the saved addresses and preselects the default
        - selects whichever card she taps
        - shows the empty prompt when she has no address, and adding one selects it
        - offers "Add new" alongside the saved list

apps/mobile/src/components/__tests__/BottomNav.test.tsx  (5 tests)
    BottomNav
        - renders the four Uber-style tabs
        - navigates to the services hub
        - navigates to bookings for Activity and mother-profile for Account when signed in
        - gates Activity and Account behind the register prompt for guests
        - does not gate Home or Services for guests

apps/mobile/src/components/__tests__/PostCard.test.tsx  (5 tests)
    PostCard — marketplace listings
        - shows an official listing as NannyNow with the Official badge
        - shows the seller name on a normal listing
        - flags a listing awaiting review
        - flags a rejected listing as needing changes
        - shows no moderation chip on a live listing

apps/mobile/src/components/__tests__/RatingPromptHost.test.tsx  (1 test)
    - runs detection and renders the sheet

apps/mobile/src/components/__tests__/RatingPromptSheet.integration.test.tsx  (1 test)
    RatingPromptSheet (real API pipeline integration)
        - treats a real axios 409 "already reviewed" response as success and closes, via the unmocked unwrap/getApiErrorMessage pipeline

apps/mobile/src/components/__tests__/RatingPromptSheet.test.tsx  (4 tests)
    RatingPromptSheet
        - renders nothing when no booking is pending
        - shows the nanny name when a booking is pending
        - disables submit until a star is chosen, then submits the rating
        - treats an "already reviewed" 409 as success and closes

apps/mobile/src/components/booking/__tests__/BookingAddressCard.test.tsx  (4 tests)
    BookingAddressCard
        - renders nothing for a booking with no address
        - shows only the area, and says why, while the address is withheld
        - shows the whole address, the door details and the landmark once revealed
        - opens the pin in the maps app

apps/mobile/src/components/ui/__tests__/star-rating-input.test.tsx  (4 tests)
    StarRatingInput
        - renders five star buttons
        - calls onChange with the tapped star value
        - shows the label for the current rating when showLabel is set
        - hides the label when showLabel is false

apps/mobile/src/hooks/__tests__/useAddresses.test.tsx  (5 tests)
    useAddresses
        - lists the address book from GET /addresses
    defaultAddressOf
        - picks the default, falling back to the first entry, or null when empty
    mutations keep the cached list current
        - useCreateAddress appends the created row (and demotes the old default when it is the new one)
        - useUpdateAddress replaces the edited row in place
        - useSetDefaultAddress and useDeleteAddress take the list the server answers with

apps/mobile/src/hooks/__tests__/useAuth.signOut.test.tsx  (5 tests)
    useSignOut
        when push is available (a native build)
            - deletes this device's push token before signing out
            - still signs out when the backend rejects the delete
            - does not call the backend when the device has no token
        when push is unavailable (Expo Go / no native module)
            - signs out without touching messaging or the devices endpoint
        - clears the cached profile once sign-out succeeds

apps/mobile/src/hooks/__tests__/useCommunity.rsvp.test.tsx  (2 tests)
    useToggleEventRsvp
        - tells the parent why when the server refuses the RSVP
        - shows nothing when the RSVP goes through

apps/mobile/src/hooks/__tests__/usePendingRating.test.tsx  (8 tests)
    pickPendingRating
        - returns null for an empty list
        - returns the completed booking when it has no review
        - returns null when the most recent completed booking is already reviewed
        - ignores bookings the user already resolved this session
        - only considers the first (most recent) item
    usePendingRating (hook behavior)
        - does not fetch or prompt for a non-mother (auth/role gate)
        - shows the prompt for a mother with an unrated completed booking
        - does not override an already-showing prompt

apps/mobile/src/hooks/__tests__/usePushNotifications.permission.test.ts  (3 tests)
    requestPushPermissionAndRegister
        - registers the device with the backend once the user allows notifications
        - registers nothing when the user denies, and does not throw
        - never lets a backend failure block the flow

apps/mobile/src/hooks/__tests__/usePushNotifications.routing.test.ts  (4 tests)
    isBookingCompletedPush
        - matches the backend push type string
        - matches the enum-cased type defensively
        - is false for other types
        - is false for missing data

apps/mobile/src/lib/__tests__/bookingDraft.test.ts  (15 tests)
    parseChildrenParam — a hand-edited deep link can never be trusted
        - parses a well-formed param and fills in the schema defaults
        - rejects the WHOLE list when any child is unpriceable, degrading to none
        - returns [] for malformed JSON rather than throwing
        - returns [] for a missing or empty param
    parseSkillIdsParam — tolerant of a mangled add-ons param
        - parses a clean comma-separated list
        - trims whitespace and drops non-positive, non-integer, and junk ids
        - returns [] for a missing or empty param
    hasRequiredBookingDraft — the broadcast flow needs date, window and address
        - is satisfied by a date, a start/end window and an address, with no nanny chosen
        - is not satisfied when any of the four is missing
    getBookingDurationHours
        - reads the carried duration, defaulting to 0 when absent
    payBookingParams — reopen checkout on an already-created booking
        - drops the timezone offset so the wall-clock fields stay offset-free
        - marks it as a retry of the existing booking and assembles the nanny name
        - omits the address for a booking that has none
        - omits nanny fields for an unclaimed (broadcast) booking
    bookingFlowRetryParams — carry the draft back into checkout
        - stamps the booking id and retry flag while preserving the flow fields

apps/mobile/src/lib/__tests__/bookingStartTimes.test.ts  (5 tests)
    bookable days — full-day care window
        - still offers today at 22:45, when only part-hour starts are left
        - offers every five-minute start left in the day, first one first
        - drops a day once even its last minute has passed
    bookable days — 06:00–22:00 care window
        - offers a start that still ends before the window closes
        - drops the day when the shortest booking would run past the close

apps/mobile/src/lib/__tests__/cancellationWarning.test.ts  (4 tests)
    cancellationWarning
        - names the window the server published
        - uses the singular for a one-hour window
        - says cancelling is free when the window is zero
        - falls back to the platform default while the options are still loading

apps/mobile/src/lib/__tests__/googlePlaces.test.ts  (5 tests)
    placeDetails
        - asks for address_components and returns the parsed parts with the pin
    reverseGeocodeDetailed
        - prefers the first street_address result over the plus code Google lists first
        - falls back to the first result when nothing resolves to a street
        - returns null on a non-OK status
    reverseGeocode (string form, used by registration)
        - still answers with just the preferred formatted line

apps/mobile/src/lib/__tests__/paymobCheckout.test.ts  (4 tests)
    buildPaymobCheckoutUrl
        - points at Paymob when nothing is configured
        - points at Paymob when the override is the empty string a real build ships
        - uses the configured origin for end-to-end runs
        - tolerates a trailing slash on the configured origin

apps/mobile/src/lib/__tests__/storage.test.ts  (8 tests)
    uploadImageToFirebase
        - uploads through the native storage module, which carries the session token
        - scopes the object path to the signed-in uid
        - returns the download URL for the uploaded object
        - refuses to upload when nobody is signed in
        - derives the extension and content type from the file name
        - normalises .jpeg to .jpg and falls back to jpg for an extensionless URI
    isLocalImageUri
        - treats %s as device-local  [parameterised]
        - treats an already-uploaded https URL as remote

apps/mobile/src/screens/auth/__tests__/RegistrationStep1Screen.test.tsx  (8 tests)
    RegistrationStep1Screen — availability check on Continue
        - asks the API with the normalised email and E.164 phone, then moves on when both are free
        - flags a taken email under the field and stays put
        - flags a taken phone under the field and stays put
        - flags both at once
        - clears a field error as soon as that field is edited
        - stays on the screen with the API error when the check itself fails
        - falls back to generic copy when the failure is technical, and still stays put
        - does not call the API while local validation is still failing

apps/mobile/src/screens/auth/__tests__/RoleSelectionScreen.test.tsx  (3 tests)
    RoleSelectionScreen
        - throws a half-typed draft away when a different role is chosen
        - starts every attempt from a clean draft, even for the same role
        - does nothing until a role is picked

apps/mobile/src/screens/auth/__tests__/VerifyEmailScreen.test.tsx  (7 tests)
    VerifyEmailScreen
        - says the phone number stays the way in, and asks for no password
        - walks address → code → confirmed, then leaves for the role router
        - refuses to send to an address that is not one
        - stays on the first pane when the send fails
        - keeps her on the screen when the code is wrong
        - surfaces the error the submit hook reported
        - is not dismissable — the only way past it is signing out

apps/mobile/src/screens/parent/__tests__/AccountScreen.test.tsx  (5 tests)
    Account screen
        - shows the big name header and verified pill
        - shows Member since year when not verified
        - routes the quick tiles
        - routes the promo cards with returnTo mother-profile
        - signs out from the list section

apps/mobile/src/screens/parent/__tests__/AddressesScreen.test.tsx  (5 tests)
    AddressesScreen
        - lists every address, marking the default, with the landmark she wrote
        - opens the form to add, and to edit the tapped address
        - makes an address the default
        - asks before deleting, then removes the address
        - shows an empty state when she has no address yet

apps/mobile/src/screens/parent/__tests__/BookingDatePickerScreen.test.tsx  (2 tests)
    BookingDatePickerScreen — late-evening availability
        - still offers today at 22:45 when care runs around the clock
        - lands on the first start still on offer, not on a passed hour

apps/mobile/src/screens/parent/__tests__/BookingHistoryScreen.test.tsx  (2 tests)
    BookingHistoryScreen
        - explains an empty tab rather than showing a blank list
        - asks the server again when pulled down

apps/mobile/src/screens/parent/__tests__/BookingStep1Screen.test.tsx  (5 tests)
    BookingStep1Screen — price
        - shows the base total the mother will pay (2h × EGP 100)
        - sends her back when a deep link lands here without any children
    BookingStep1Screen — Care Points redemption
        - offers redemption when the wallet covers at least one free hour
        - hides redemption when the balance is short of one free hour
        - reserving a free hour drops what she pays by one hour of care

apps/mobile/src/screens/parent/__tests__/CommunityFeedScreen.test.tsx  (2 tests)
    CommunityFeedScreen
        - explains an empty feed rather than showing nothing
        - asks the server again when pulled down

apps/mobile/src/screens/parent/__tests__/CustomerSupportScreen.test.tsx  (8 tests)
    CustomerSupportScreen contact channels
        - opens a wa.me link for the configured WhatsApp number
        - dials the configured support number
        - opens a mailto link for the configured support email
        - hides a channel the admin has not configured
        - shows only the community card when nothing is configured
        - keeps the community card when the request fails
    CustomerSupportScreen FAQ
        - shows the questions the operator wrote, first one open
        - filters the questions by the search box

apps/mobile/src/screens/parent/__tests__/HomeScreen.test.tsx  (4 tests)
    HomeScreen
        - shows the live-order card and lets a signed-in mother start a booking
        - greets a guest with the welcome card instead of the live-order card
        - turns a guest’s "Book care" into the create-account prompt
        - reloads everything on screen when pulled down

apps/mobile/src/screens/parent/__tests__/MyListingsScreen.test.tsx  (4 tests)
    MyListingsScreen
        - shows the rejection reason and an edit-and-resubmit action
        - marks a listing awaiting review and offers no resubmit copy
        - shows a live listing without an edit prompt
        - explains the empty state

apps/mobile/src/screens/parent/__tests__/PostDetailScreen.test.tsx  (2 tests)
    PostDetailScreen — marketplace CTA
        - opens WhatsApp on the listing’s number for an official listing
        - keeps in-app messaging for a seller listing

apps/mobile/src/screens/parent/__tests__/ServicesHubScreen.test.tsx  (3 tests)
    ServicesHubScreen
        - shows the hero and the five service tiles
        - navigates to each destination
        - gates booking and Care Points for guests but leaves browsing open

apps/mobile/src/screens/parent/__tests__/watch-live-gating.test.ts  (3 tests)
    canWatchLive
        - shows the button during the shift when a camera exists
        - hides the button when the nanny has no camera
        - hides the button while the booking is %s, even with a camera  [parameterised]

apps/mobile/src/store/__tests__/ratingPromptStore.test.ts  (4 tests)
    ratingPromptStore
        - starts with no booking
        - showRatingPrompt sets the booking
        - clearRatingPrompt resets the booking
        - tracks resolved bookings

================================================================================================
MOBILE APP — end-to-end device flows (Maestro, Android emulator)
18 tests in 18 files
================================================================================================

a01-booking-happy-path.yaml
    - A1 — booking happy path, card payment. The flagship flow.

a04-promo-code.yaml
    - A4 — promo code end to end.

a05-care-points.yaml
    - A5 — Care Points redemption.

a06-package-purchase.yaml
    - A6 — package purchase → hours → consumption.

a07-extension.yaml
    - A7 — mid-care extension.

a08-adjustment-checkout.yaml
    - A8 — the mother settles the balance our team's edit created.

a10-nanny-onboarding.yaml
    - A10 — a nanny signs up through the forms, waits for vetting, then is approved.

a11-mother-id-gate.yaml
    - A11 — a mother's ID gates booking.

c01-session-lifecycle.yaml
    - C1 — getting in, getting out, and the door marked "Forgot password?".

c02-mother-registration.yaml
    - C2 — a mother signs up from scratch, all the way to her home screen.

c04-nanny-day.yaml
    - C4 — a nanny's day: dashboard → requests → claim → start → care log.

c05-community.yaml
    - C5 — the community: ask, like, comment, host, RSVP, hit the ceiling, and list.

c06-messaging.yaml
    - C6 — messaging: the inbox, the badge, the thread, and the reply.

c07-referral.yaml
    - C7 — referral: the code, the invite it earned, and the check a new signup runs.

c08-notification-centre.yaml
    - C8 — the notification centre: what is in it, what the count says, and what

c09-customer-support.yaml
    - C9 — help and support: the channels the admin configured, and the FAQ.

c10-failure-states.yaml
    - C10 — what the app says when there is nothing to show, and when it cannot ask.

smoke.yaml
    - Proves the whole chain, and nothing more: Maestro drives the app, the app
